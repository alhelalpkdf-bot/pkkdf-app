# PKKDF কৃষক MIS + অফলাইন PWA — ডেপ্লয়মেন্ট গাইড

## ১. আর্কিটেকচার (গুরুত্বপূর্ণ — আগে পড়ুন)

Google Apps Script দিয়ে সরাসরি একটি PWA হোস্ট করা যায় **না**, কারণ Service
Worker-কে অবশ্যই সেই একই origin/path থেকে সার্ভ হতে হয় যেটার সে scope
নিয়ন্ত্রণ করবে — Apps Script-এর ওয়েব অ্যাপ URL (`.../macros/s/…/exec`)
এই শর্ত পূরণ করে না, তাই Service Worker রেজিস্টার হবে না এবং অফলাইন
কাজ করবে না।

তাই এই সিস্টেমে দুটি অংশ আলাদা:

| অংশ | প্রযুক্তি | দায়িত্ব |
|---|---|---|
| **Backend (API)** | Google Apps Script → **Web App** হিসেবে ডেপ্লয় | Google Sheets-এ ডেটা CRUD, ছবি Drive-এ সংরক্ষণ, JSON রিটার্ন |
| **Frontend (PWA)** | `index.html`, `style.css`, `app.js`, `manifest.json`, `service-worker.js` → **Firebase Hosting / GitHub Pages**-এ হোস্ট | UI, IndexedDB অফলাইন সংরক্ষণ, Service Worker, ফিল্ড অফিসারের ব্যবহারের অংশ |

Frontend প্রতিটি রিকোয়েস্টে `fetch()` দিয়ে Apps Script API-কে কল করে।
এটাই বাস্তবে প্রফেশনাল প্রোডাকশন সেটআপ — Apps Script-কে সবসময় ব্যাকএন্ড
API হিসেবে ব্যবহার করা উচিত, স্ট্যাটিক ফাইল সার্ভার হিসেবে নয়।

---

## ২. Google Sheet তৈরি করুন

একটি নতুন Google Sheet খুলুন এবং নিচের নামে ৫টি ট্যাব (শিট) তৈরি করুন:

### `Employees`
| nid | mobile | pin | id | name | designation | officeName | region | role | active |
|---|---|---|---|---|---|---|---|---|---|

**নতুন কলাম যোগ হয়েছে (৩০০+ ইউজার, ১৬ অ্যাডমিন, ৩ সুপার অ্যাডমিনের জন্য):**
- `role` — অবশ্যই এই তিনটার একটা লিখবেন: `officer`, `admin`, অথবা `super_admin`
- `region` — শুধু `admin`-দের জন্য পূরণ করুন: সেই অ্যাডমিন যে জেলার (district) দায়িত্বে
  আছেন সেটা লিখুন (`Farmers` শিটের `district` কলামের বানানের সাথে হুবহু মিলতে হবে)।
  `officer` আর `super_admin` রো-তে এটা খালি রাখতে পারেন।
- সাধারণ ফিল্ড অফিসারদের রো-তে `role` খালি রাখলে সিস্টেম ধরে নেবে সে `officer`

**অ্যাক্সেসের নিয়ম (কোডে বসানো আছে):**
- `officer` → শুধু নিজের রেজিস্টার করা কৃষকদের ডেটা দেখবে
- `admin` → তার `region`-এ (জেলায়) থাকা সব অফিসারের ডেটা দেখবে, এডিট করতে পারবে, কিন্তু
  অন্য এলাকার ডেটা দেখবে না, আর `super_admin` রোল কাউকে দিতে পারবে না
- `super_admin` → সব ডেটা দেখা, সব অফিসার/অ্যাডমিন যোগ-বাদ-এডিট, যেকোনো রেকর্ড ডিলিট

⚠️ **সততার সাথে একটা সীমাবদ্ধতা জানানো দরকার:** Apps Script-এর "Anyone" অ্যাক্সেসে
প্রকৃত লগইন সেশন/টোকেন থাকে না। অ্যাপ প্রতিটা অনুরোধে `requesterId` পাঠায় আর সার্ভার
Employees শিট দেখে সেই আইডির role চেক করে — এটা অ্যাপের ভেতর থেকে ভুলবশত
অন্যের ডেটা দেখা/মোছা আটকায়, কিন্তু কেউ ইচ্ছা করে টেকনিক্যাল উপায়ে `requesterId`
জাল করলে সেটা পুরোপুরি ঠেকানো এই আর্কিটেকচারে সম্ভব না। ৩০০+ ইউজার ও সরকারি
সংবেদনশীল ডেটার জন্য ভবিষ্যতে Firebase Authentication বা সমতুল্য প্রকৃত
টোকেন-ভিত্তিক auth যোগ করার পরিকল্পনা রাখুন।

### `Locations`
| district | upazila | union | village |
|---|---|---|---|

### `Crops`
| cropName |
|---|

### `Farmers`
কোনো হেডার লেখার দরকার নেই — প্রথম `submitFarmer` কল হলে `Code.gs`
স্বয়ংক্রিয়ভাবে সঠিক হেডার বসিয়ে দেবে (`FARMER_COLUMNS` দেখুন)।

### `SyncLog`
| timestamp | action | formNo | deviceId |
|---|---|---|---|

---

## ৩. Apps Script ব্যাকএন্ড ডেপ্লয় করুন

1. Sheet খুলুন → এক্সটেনশন → Apps Script
2. `Code.gs` ফাইলটির পুরো কন্টেন্ট কপি-পেস্ট করুন (এই প্রজেক্টের `Code.gs` ফাইল)
3. উপরে ডানদিকে **Deploy → New deployment**
4. Type নির্বাচন করুন: **Web app**
5. সেটিংস:
   - Execute as: **Me**
   - Who has access: **Anyone** (ফিল্ড অফিসাররা লগইন ছাড়াই কল করবে; নিরাপত্তা NID+PIN দিয়ে অ্যাপ লেভেলে হ্যান্ডল হয়)
6. Deploy চাপুন, প্রথমবার Drive/Sheets পারমিশন অনুমোদন করুন
7. যে URL পাবেন (…/exec দিয়ে শেষ হয়) সেটি কপি করুন

---

## ৪. Frontend-এ API URL বসান

`app.js` ফাইল খুলুন, একদম উপরে:

```js
const CONFIG = {
  API_BASE: 'https://script.google.com/macros/s/এখানে_আপনার_ID/exec',
  OFFICE_LAT: 23.8103,   // আপনার অফিসের লোকেশন বসান
  OFFICE_LNG: 90.4125,
  ...
};
```

---

## ৫. Frontend হোস্ট করুন (Firebase Hosting — সহজ ও ফ্রি)

```bash
npm install -g firebase-tools
firebase login
firebase init hosting     # public directory: এই ফোল্ডার (যেখানে index.html আছে)
firebase deploy
```

Firebase একটি `https://your-app.web.app` URL দেবে — এটাই আপনার PWA লিংক,
যা HTTPS-এ থাকে (PWA ইনস্টলের জন্য বাধ্যতামূলক)।

**বিকল্প: GitHub Pages** — রিপোতে ফাইলগুলো পুশ করুন, Settings → Pages →
branch নির্বাচন করলেই হোস্ট হয়ে যাবে।

---

## ৬. মোবাইলে ইনস্টল (ফিল্ড অফিসারদের জন্য)

1. মোবাইল ক্রোমে হোস্টেড লিংক খুলুন
2. মেনু → **"Add to Home screen" / "হোম স্ক্রিনে যোগ করুন"**
3. একবার খোলার পরে অ্যাপ শেল (HTML/CSS/JS + আইকন) ক্যাশে হয়ে যায় —
   এরপর থেকে ইন্টারনেট ছাড়াই অ্যাপ খুলবে
4. প্রথমবার লগইন করতে অবশ্যই ইন্টারনেট লাগবে (Employees শিট যাচাইয়ের জন্য);
   এরপর PIN স্থানীয়ভাবে ক্যাশ থাকে বলে অফলাইনেও লগইন করা যায়

---

## ৭. অফলাইন সিঙ্ক কীভাবে কাজ করে

- ফর্ম সাবমিট হলে সাথে সাথে **IndexedDB**-তে সংরক্ষিত হয় (ছবিসহ, কম্প্রেসড JPEG হিসেবে)
- ইন্টারনেট না থাকলে `queue` স্টোরে যোগ হয়, উপরের ব্যানারে
  "X টি ফর্ম সিঙ্কের অপেক্ষায়" দেখায়
- `window.addEventListener('online', ...)` ট্রিগার হলে `syncBatch`
  অ্যাকশনের মাধ্যমে সবগুলো Apps Script-এ পাঠানো হয়, একে একে; সফল
  হওয়াগুলো লোকাল queue থেকে মুছে যায়, ব্যর্থগুলো পরের বার আবার চেষ্টা হয়
- কোনো ডেটা মোবাইলে বন্ধ হয়ে গেলেও হারাবে না — পরের বার অ্যাপ খুললে
  queue-এ থাকা ডেটা আবার সিঙ্কের চেষ্টা করবে

---

## ৮. আইকন পরিবর্তন

`icons/` ফোল্ডারে PKKDF-এর মনোগ্রাম থেকে তৈরি ৯৬/১৯২/৫১২ পিক্সেলের
আইকন দেওয়া আছে। নতুন লোগো দিলে একই সাইজে PNG বানিয়ে একই নামে
প্রতিস্থাপন করুন।

---

## ৯. প্রয়োজনীয় Google পারমিশন

- Google Sheets API (স্বয়ংক্রিয় — Apps Script প্রজেক্টের সাথেই থাকে)
- Google Drive (ছবি সংরক্ষণের জন্য) — প্রথম ডেপ্লয়ে অনুমতি চাইবে

---

## ৩০০+ ইউজারের জন্য যা যোগ করা হয়েছে

- **Role-based access:** `Employees` শিটে `role` (officer/admin/super_admin) ও
  `region` কলাম যোগ হয়েছে। officer শুধু নিজের ডেটা দেখে, admin তার region-এর
  সব ডেটা দেখে, super_admin সব দেখে + কর্মী ম্যানেজ করতে পারে।
- **Concurrent-write সুরক্ষা:** অনেক অফিসার একসাথে সিঙ্ক করলে ডেটা যেন
  ওভাররাইট না হয়ে যায়, তাই প্রতিটা লেখার (write) সময় `LockService` ব্যবহার
  করা হয়েছে — একজনের লেখা শেষ না হওয়া পর্যন্ত পরেরজনের অপেক্ষা করে।
- **অ্যাডমিন প্যানেল (অ্যাপে নতুন ট্যাব):** শুধু admin/super_admin লগইন করলে
  নিচে "অ্যাডমিন" ট্যাব দেখা যাবে — কর্মী তালিকা, নতুন কর্মী যোগ, সক্রিয়/নিষ্ক্রিয়
  করা, এবং নিজের এলাকার কৃষক রেকর্ড ডিলিট করার সুবিধা।

## সীমাবদ্ধতা যা মাথায় রাখবেন

- Apps Script কোটা: প্রতিদিন doPost/doGet কলের একটা সীমা আছে (Google
  Workspace অ্যাকাউন্টে বেশি, ব্যক্তিগত Gmail-এ কম) — অনেক ফিল্ড
  অফিসার একসাথে ব্যবহার করলে Apps Script-কে ভারী ট্রাফিকের জন্য নয়,
  তাই বড় পরিসরে গেলে Firebase Functions/Cloud SQL-এ মাইগ্রেশন বিবেচনা করুন।
- ছবি Google Drive-এ সংরক্ষিত হয় এবং শেয়ারেবল লিংক তৈরি হয় — প্রতিষ্ঠানের
  Drive স্টোরেজ কোটা মনিটর করুন।
